# Test configuration for GKE service account tests
# This file allows you to modify GCP parameters without changing the code

# GCP Configuration
gcp:
  project_id: "test-project-id"
  region: "us-central1"
